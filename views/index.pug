extends layout.pug

block content

    h1.feed-main-header What's popular on Mezzo right now
    .feed-main-underline
    body
        div.preview
            each story in stories
                img.feed-story-image(src=story.image)
                h1=story.title
                p=story.subtitle
                p=story.content
